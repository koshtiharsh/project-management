// import nextPWA from 'next-pwa';

// /** @type {import('next').NextConfig} */
// const nextConfig = nextPWA({
//     dest: 'public', // Change this to '.next/static' if it's not working
//     register: true,
//     skipWaiting: true,
// });

// export default nextConfig;
/** @type {import('next').NextConfig} */
const nextConfig = {};

export default nextConfig;